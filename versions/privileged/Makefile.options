# Image
TAG := privileged

# Build Args
BUILD_PARAMS := --build-arg DUID="${DUID}" --build-arg DGID="${DGID}" --build-arg USER="root" --build-arg MINIFY_URL="github.com/tdewolff/minify/cmd/minify"

# Filepath
PATH_DOCKER := $(PATH_ROOT)/src
PATH_DOCKERFILE := $(PATH_DOCKER)/Dockerfile